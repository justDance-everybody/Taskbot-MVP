# Bug修复：多维表格缺少"分配给"字段

**修复时间**: 2026-01-06  
**问题**: 任务分配时报错 `FieldNameNotFound: 'fields.分配给'`

---

## 🐛 问题描述

当用户选择候选人分配任务时，系统报错：

```
2026-01-06 12:41:10,033 - app.bitable - ERROR - 请求处理失败: Request failed: 
{'code': 1254045, 'msg': 'FieldNameNotFound', 
 'error': {'message': "Invalid request parameter: 'fields.分配给'. 
           Correct format : field_name not found."}}
```

用户收到消息：`❌ 任务分配失败，请稍后重试`

---

## 🔍 问题分析

### 根本原因

**多维表格中缺少"分配给"字段**，但代码尝试更新这个不存在的字段。

### 实际表格字段

通过检查发现，任务表只有以下 8 个字段：

```
✅ 任务ID
✅ 任务标题  
✅ 任务描述
✅ 任务状态
✅ 创建人
✅ 截止时间
✅ 技能标签
✅ 紧急程度

❌ 分配给 (缺失)
```

### 代码中的错误使用

```python
# webhooks.py - handle_candidate_selection()
update_data = {
    '任务状态': 'assigned',
    '分配给': [{"id": candidate_id}]  # ❌ 字段不存在
}
```

---

## ✅ 临时修复方案

### 修改代码，移除对"分配给"字段的依赖

**修改文件**: `app/webhooks.py`

#### 1. 修复 `handle_candidate_selection()` 函数

**修改前**:
```python
update_data = {
    '任务状态': 'assigned',
    '分配给': [{"id": candidate_id}]
}
```

**修改后**:
```python
# 注意：多维表格中暂时没有"分配给"字段，只更新状态
update_data = {
    '任务状态': 'assigned'
    # '分配给': [{"id": candidate_id}]  # 待添加字段后启用
}
```

#### 2. 修复 `handle_assign_confirm()` 函数

**修改前**:
```python
update_data = {
    '任务状态': 'assigned',
    '分配给': [{"id": candidate_id}]  # 人员字段格式
}
```

**修改后**:
```python
# 注意：多维表格中暂时没有"分配给"字段，只更新状态
update_data = {
    '任务状态': 'assigned'
    # '分配给': [{"id": candidate_id}]  # 待添加字段后启用
}
```

---

## 🎯 完整解决方案（推荐）

### 在多维表格中添加"分配给"字段

#### 步骤 1: 打开多维表格

1. 在飞书中打开任务管理的多维表格
2. 找到"任务表"

#### 步骤 2: 添加新字段

1. 点击表格右侧的 **"+"** 按钮（添加字段）
2. 配置字段信息：
   - **字段名称**: `分配给` 或 `负责人`
   - **字段类型**: 选择 **"人员"**
   - **是否必填**: 否（可选）
   - **是否允许多选**: 否（单选）

3. 点击"确定"保存

#### 步骤 3: 启用代码中的字段

修改 `app/webhooks.py`，取消注释：

```python
update_data = {
    '任务状态': 'assigned',
    '分配给': [{"id": candidate_id}]  # ✅ 启用
}
```

#### 步骤 4: 重启服务

```bash
# 重启应用
python main.py
```

---

## 📊 字段配置详情

### 推荐的字段配置

| 配置项 | 值 |
|--------|-----|
| 字段名称 | `分配给` 或 `负责人` |
| 字段类型 | 人员 (User) |
| 是否必填 | 否 |
| 允许多选 | 否 |
| 默认值 | 无 |

### 字段类型说明

飞书多维表格的"人员"字段类型：
- 存储格式：`[{"id": "ou_xxx", "name": "张三"}]`
- API 更新格式：`[{"id": "ou_xxx"}]`
- 支持通过 open_id 或 user_id 引用用户

---

## 🧪 测试验证

### 临时方案测试

1. ✅ 用户发送 `/assign` 命令
2. ✅ 选择任务
3. ✅ 选择候选人
4. ✅ 任务状态更新为 `assigned`
5. ✅ 创建协作群成功
6. ⚠️ 但无法在表格中看到分配的候选人

### 完整方案测试

添加字段后：

1. ✅ 用户发送 `/assign` 命令
2. ✅ 选择任务
3. ✅ 选择候选人
4. ✅ 任务状态更新为 `assigned`
5. ✅ "分配给"字段显示候选人信息
6. ✅ 创建协作群成功

---

## 📝 影响分析

### 临时方案的影响

**优点**:
- ✅ 立即修复错误
- ✅ 任务分配功能可用
- ✅ 协作群创建正常

**缺点**:
- ⚠️ 无法在表格中看到任务分配给谁
- ⚠️ 需要通过任务状态和协作群判断
- ⚠️ 统计功能可能受影响

### 完整方案的影响

**优点**:
- ✅ 完整的任务分配记录
- ✅ 可在表格中直接查看负责人
- ✅ 支持按负责人筛选任务
- ✅ 统计功能完整

**缺点**:
- ⚠️ 需要手动添加字段
- ⚠️ 需要重启服务

---

## 🔧 相关代码位置

### 修改的文件
- `app/webhooks.py` (2处修改)
  - `handle_candidate_selection()` - AI推荐分配
  - `handle_assign_confirm()` - 手动分配确认

### 相关文件
- `app/bitable.py` - 字段映射定义
- `app/services/task_manager.py` - 任务管理服务

### 检查工具
- `check_table_fields.py` - 字段检查脚本

---

## 📖 字段映射说明

### 当前的字段映射

在 `app/bitable.py` 中定义：

```python
self.field_mapping = {
    '任务ID': 'taskid',
    '任务标题': 'title',
    '任务描述': 'description',
    '创建人': 'creator',
    '创建时间': 'create_time',
    '任务状态': 'status',
    '技能标签': 'skilltags',
    '截止时间': 'deadline',
    '紧急程度': 'urgency',
    '分配给': 'assignee',  # ← 映射存在，但字段不存在
    '最终评分': 'final_score',
    '完成时间': 'completed_at'
}
```

### 建议的字段名称

可以使用以下任一名称（需要在代码中统一）：
- `分配给` (推荐，与代码一致)
- `负责人`
- `承接人`
- `assignee` (英文)

---

## 🚀 后续优化建议

### 短期优化
1. ✅ 添加"分配给"字段到多维表格
2. ✅ 启用代码中的字段更新
3. ✅ 测试完整的分配流程
4. ✅ 更新文档说明

### 长期优化
1. 添加字段存在性检查
2. 自动创建缺失的字段
3. 提供字段配置向导
4. 完善错误提示信息

### 代码改进
```python
# 建议：添加字段检查
async def update_task_with_assignee(task_id, candidate_id):
    """更新任务并分配候选人"""
    update_data = {'任务状态': 'assigned'}
    
    # 检查字段是否存在
    if await check_field_exists('分配给'):
        update_data['分配给'] = [{"id": candidate_id}]
    else:
        logger.warning("字段'分配给'不存在，跳过更新")
    
    return await bitable_client.update_task(task_id, update_data)
```

---

## ✅ 验证清单

### 临时方案
- [x] 移除"分配给"字段的更新
- [x] 保留任务状态更新
- [x] 测试任务分配流程
- [x] 创建修复文档

### 完整方案（待执行）
- [ ] 在多维表格中添加"分配给"字段
- [ ] 配置字段类型为"人员"
- [ ] 启用代码中的字段更新
- [ ] 重启服务
- [ ] 测试完整流程
- [ ] 验证表格中显示正确

---

## 📞 操作指南

### 如何添加"分配给"字段

**图文步骤**:

1. **打开多维表格**
   - 在飞书中找到任务管理多维表格
   - 打开"任务表"

2. **添加字段**
   - 点击表格最右侧的 "+" 图标
   - 或点击任意列标题 → "在右侧插入列"

3. **配置字段**
   ```
   字段名称: 分配给
   字段类型: 人员 ← 重要！
   是否必填: 否
   允许多选: 否
   ```

4. **保存并验证**
   - 点击"确定"
   - 检查字段是否出现在表格中

5. **启用代码**
   - 编辑 `app/webhooks.py`
   - 取消注释 `'分配给': [{"id": candidate_id}]`
   - 重启服务

---

## 📊 预期效果

### 添加字段前
```
任务表:
┌────────┬──────┬────────┬────────┐
│ 任务ID │ 标题 │ 状态   │ ...    │
├────────┼──────┼────────┼────────┤
│ T001   │ XXX  │assigned│ ...    │  ← 看不出分配给谁
└────────┴──────┴────────┴────────┘
```

### 添加字段后
```
任务表:
┌────────┬──────┬────────┬────────┬────────┐
│ 任务ID │ 标题 │ 状态   │ 分配给 │ ...    │
├────────┼──────┼────────┼────────┼────────┤
│ T001   │ XXX  │assigned│ @张三  │ ...    │  ← 清晰显示
└────────┴──────┴────────┴────────┴────────┘
```

---

**修复状态**: ✅ 临时方案已完成  
**完整方案**: ⏳ 待添加字段  
**文档状态**: ✅ 已更新

---

**相关文档**:
- [BUGFIX_ASSIGN_COMMAND.md](BUGFIX_ASSIGN_COMMAND.md) - /assign 命令字段名修复
- [BUGFIX_CARD_CALLBACK_CHATID.md](BUGFIX_CARD_CALLBACK_CHATID.md) - 卡片回调 chat_id 修复
- [README.md](README.md) - 多维表格配置说明
