# 飞书应用配置
FEISHU_APP_ID=<REPLACE_ME>
FEISHU_APP_SECRET=<REPLACE_ME>
FEISHU_VERIFY_TOKEN=<REPLACE_ME>
FEISHU_ENCRYPT_KEY=<REPLACE_ME>

# LLM 模型配置
DEEPSEEK_KEY=<REPLACE_ME>
GEMINI_KEY=<REPLACE_ME>
OPENAI_KEY=<REPLACE_ME>
OPENAI_BASE_URL=<REPLACE_ME>
OPENAI_MODEL=<REPLACE_ME>
GEMINI_MODEL=<REPLACE_ME>

# GitHub Webhook 配置
GITHUB_WEBHOOK_SECRET=<REPLACE_ME>
GITHUB_TOKEN=<REPLACE_ME>

# 服务配置
SERVER_HOST=<REPLACE_ME>
SERVER_PORT=<REPLACE_ME>
DEBUG=<REPLACE_ME>
ENVIRONMENT=<REPLACE_ME>
WORKERS=<REPLACE_ME>
RELOAD=<REPLACE_ME>

# 飞书多维表格配置
FEISHU_BITABLE_APP_TOKEN=<REPLACE_ME>
FEISHU_TASK_TABLE_ID=<REPLACE_ME>
FEISHU_PERSON_TABLE_ID=<REPLACE_ME>

# 数据库配置（可选）
DATABASE_URL=<REPLACE_ME>

# Redis配置（可选）
REDIS_URL=<REPLACE_ME>
REDIS_PASSWORD=

# 日志配置
LOG_LEVEL=<REPLACE_ME>
LOG_FILE=<REPLACE_ME>
LOG_MAX_SIZE=<REPLACE_ME>
LOG_BACKUP_COUNT=<REPLACE_ME>

# 安全配置
SECRET_KEY=<REPLACE_ME>
ACCESS_TOKEN_EXPIRE_MINUTES=<REPLACE_ME>

# 任务配置
DEFAULT_TASK_TIMEOUT=<REPLACE_ME>
MAX_TASK_RETRIES=<REPLACE_ME>
TASK_CLEANUP_DAYS=<REPLACE_ME>

# 通知配置
DAILY_REPORT_TIME=<REPLACE_ME>
REMINDER_INTERVAL=<REPLACE_ME>
MAX_REMINDER_COUNT=<REPLACE_ME>

# 监控配置
MONITOR_INTERVAL=<REPLACE_ME>
HEALTH_CHECK_TIMEOUT=<REPLACE_ME>
ALERT_WEBHOOK_URL=

# 功能开关
ENABLE_TASK_AUTO_ASSIGNMENT=<REPLACE_ME>
ENABLE_QUALITY_CHECK=<REPLACE_ME>
ENABLE_DAILY_REPORT=<REPLACE_ME>
ENABLE_REMINDERS=<REPLACE_ME>
ENABLE_GITHUB_INTEGRATION=<REPLACE_ME>
ENABLE_METRICS=<REPLACE_ME>

# 缓存配置
CACHE_TTL=<REPLACE_ME>
CACHE_ENABLED=<REPLACE_ME>

# API限流配置
RATE_LIMIT_ENABLED=<REPLACE_ME>
RATE_LIMIT_REQUESTS=<REPLACE_ME>
RATE_LIMIT_WINDOW=<REPLACE_ME>

# 时区和语言
TIMEZONE=<REPLACE_ME>
DEFAULT_LANGUAGE=<REPLACE_ME>